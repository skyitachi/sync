{
  "name": "sync",
  "version": "0.0.3",
  "bin": {
    "sync": "./client.js"
  },
  "scripts": {
    "server": "export NODE_ENV=test && node --harmony server.js",
    "client": "export NODE_ENV=test && node --harmony client.js",
    "postinstall": "mkdir ~/.sync && cp ./config.json ~/.sync/config.json"
  },
  "dependencies": {
    "chalk": "^1.1.0",
    "commander": "^2.8.1",
    "debug": "^2.2.0",
    "http-status": "^0.2.0",
    "object-assign": "^3.0.0",
    "promise": "^7.0.3",
    "text-table": "^0.2.0"
  }
}
